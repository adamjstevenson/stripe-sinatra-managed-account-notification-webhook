# A Stripe Managed Account Identity Verification Webhook Handler

A simple Sinatra webhook handler that listens for the `account.updated` event and notifies managed accounts when information is needed. 